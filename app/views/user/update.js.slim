- broadcast "/user/update" do
  - if @error
    | $("#error_label").text("
    = @error
    | ");
    | $("#error_row").show();
  - else
    - if @user
      | $("##{@user.id}").text("#{@user.task}");
      | $("##{@user.id}").val("#{@user.task}");
